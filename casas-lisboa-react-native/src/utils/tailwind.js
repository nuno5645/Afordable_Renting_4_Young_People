import { create } from 'nativewind';
import { StyleSheet } from 'react-native';

// Create the styled utility
const { styled, css } = create(StyleSheet);

// Export the styled HOC and css function
export { styled, css }; 